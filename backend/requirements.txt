# Production dependencies for Phase II
# Web framework and database
fastapi>=0.104.0
sqlmodel>=0.0.14
# Note: uvicorn not needed for Vercel serverless

# PostgreSQL driver
psycopg2-binary>=2.9.0

# Database migrations
alembic>=1.13.0

# Configuration
python-dotenv>=1.0.0
pydantic>=2.5.0
pydantic-settings>=2.0.0

# JWT Authentication (Phase II Step 2)
PyJWT>=2.10.0

# Testing (Phase III)
pytest>=7.4.0
pytest-asyncio>=0.21.0

# MCP Server (Phase III - Tools Layer)
mcp>=1.0.0
