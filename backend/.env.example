# Phase II Step 1 - REST API Configuration
# Copy this file to .env and fill in actual values

# Database Configuration
# Get your connection string from Neon dashboard: https://console.neon.tech
DATABASE_URL=postgresql://user:password@host/dbname?sslmode=require

# API Settings
LOG_LEVEL=INFO
DEBUG=False

# CORS Configuration (comma-separated origins)
# Development: Allow localhost origins for testing
# Production: Specify your frontend domain
CORS_ORIGINS=http://localhost:3000,http://localhost:3001

# Server Configuration (optional - defaults shown)
# HOST=0.0.0.0
# PORT=8000

# JWT Authentication (Phase II Step 2)
# Generate a secure secret: openssl rand -hex 32
# MUST be identical in both frontend and backend
BETTER_AUTH_SECRET=your-256-bit-secret-key-here-replace-with-actual-secret
